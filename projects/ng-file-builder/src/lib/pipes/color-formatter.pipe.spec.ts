import { ColorFormatterPipe } from './color-formatter.pipe';

describe('ColorFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
